
%% 1
clear; 
close all; 
config; 
setup;
configFile   = 'camera1_exp.config';
dataset      = loadExperimentSetting(configFile, dataset);

DEMO

%% 2
clear; 
close all; 
config; 
setup;
configFile   = 'camera2_exp.config';
dataset      = loadExperimentSetting(configFile, dataset);

DEMO

%% 3
clear; 
close all; 
config; 
setup;
configFile   = 'camera3_exp.config';
dataset      = loadExperimentSetting(configFile, dataset);

DEMO

%% 4 
clear; 
close all; 
config; 
setup;
configFile   = 'camera4_exp.config';
dataset      = loadExperimentSetting(configFile, dataset);

DEMO

%% 5
clear; 
close all; 
config; 
setup;
configFile   = 'camera5_exp.config';
dataset      = loadExperimentSetting(configFile, dataset);

DEMO

%% 6
clear; 
close all; 
config; 
setup;
configFile   = 'camera6_exp.config';
dataset      = loadExperimentSetting(configFile, dataset);

DEMO

%% 7
clear; 
close all; 
config; 
setup;
configFile   = 'camera7_exp.config';
dataset      = loadExperimentSetting(configFile, dataset);

DEMO

%% 8
clear; 
close all; 
config; 
setup;
configFile   = 'camera8_exp.config';
dataset      = loadExperimentSetting(configFile, dataset);

DEMO
